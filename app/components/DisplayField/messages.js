/*
 * DisplayField Messages
 *
 * This contains all the text for the DisplayField component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DisplayField';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DisplayField component!',
  },
});
